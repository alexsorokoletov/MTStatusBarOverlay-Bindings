namespace MTStatusBarOverlayBinding
{
    public enum MTStatusBarOverlayAnimation : uint
    {
        None,
        Shrink,
        FallDown
    }

    public enum MTDetailViewMode : uint
    {
        History,
        DetailText,
        Custom
    }

    public enum MTMessageType : uint
    {
        Activity,
        Finish,
        Error
    }
}